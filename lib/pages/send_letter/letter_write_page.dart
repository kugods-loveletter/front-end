import 'package:flutter/material.dart';

class LetterWritePage extends StatelessWidget {
  const LetterWritePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("편지 작성 페이지")),
    );
  }
}
